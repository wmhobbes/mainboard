.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:slots, :index)
      %li=link_to pat(:new), url(:slots, :new)
      %li.active=link_to pat(:edit), url(:slots, :edit, :id => @slot.id)
  .content
    %h2.title
      =pat(:edit)
      =mt(:slot)
    .inner
      -form_for :slot, url(:slots, :update, :id => @slot.id), :method => :put, :class => :form do |f|
        =partial "slots/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")